import { registerRootComponent } from 'expo';
import App from './App';

// Log startup message
console.log('Starting Chess App Mobile with Expo web support');

// Register the root component
registerRootComponent(App);
